@model IEnumerable<Hotel.Models.Bedroom>

@{
    ViewBag.Title = "Master Hotel Bedroom";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BedroomType.BedRoomTypeCode)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BedroomNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AvailabilityStatus)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BedroomType.BedRoomTypeCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BedroomNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AvailabilityStatus)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}
    <p>
        @Html.ActionLink("Back to Index Admin", "IndexAdmin", "Home")
    </p>
</table>
